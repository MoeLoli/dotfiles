name: Linux

on:
  push:
    branches:
      - master

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - run: curl -o- https://raw.githubusercontent.com/SoraJin424/dotfiles/master/install/Linux.sh | bash